import { Metadata } from "next";

export const metadata: Metadata = {
  title: "ThinkCar | Contacts",
};

export default function Contacts() {
  return (
    <main className="flex-1">
      <div className="container mx-auto">Contacts</div>
    </main>
  );
}
